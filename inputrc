# Set vim to be the default editor for readline
# set editing-mode vi

$if mode=vi
  # Normal mode
  set keymap vi-command
  "H": beginning-of-line
  "L": end-of-line

  # Insert mode
  set keymap vi-insert
  # ...
$endif

