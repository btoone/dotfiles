# ~/.tmuxinator/chefalytics.yml
# you can make as many tabs as you wish...

project_name: chefalytics
project_root: ~/code/chefalytics
# pre: sudo /etc/rc.d/mysqld start
tabs:
  - shell: git status
  - editor: vim
  - guard: bundle exec guard
  - console:
  - server: bundle exec foreman start -f Procfile.dev
  - staging: 
