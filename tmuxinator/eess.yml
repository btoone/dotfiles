# ~/.tmuxinator/eess.yml
# you can make as many tabs as you wish...

project_name: eess
project_root: ~/code/eess
pre: vagrant up
tabs:
  - editor: vim
  - shell: git status
  - guard: bundle exec guard
  - routes: bundle exec rake routes | less
  - rails:
      - vagrant ssh
  - server:
      - vagrant ssh
  - production: 
