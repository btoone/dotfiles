# ~/.tmuxinator/william.yml
# you can make as many tabs as you wish...

project_name: william
project_root: ~/code/william
pre: vagrant up
tabs:
  - editor: vim
  - shell: git status
  - guard: bundle exec guard
  - routes: bundle exec rake routes | less
  - rails:
      - vagrant ssh
  - server:
      - vagrant ssh
  - production: 
